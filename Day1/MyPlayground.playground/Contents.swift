import UIKit

var greeting = "Hello, playground"
var a = 10
a + 30

// 상수 -> let와 변수 -> var을 구별

// PERL 일반적인 언어에서의 사용
// $a = 10;
// $b = "hello $b world";
// swift에서는 \(  ) 방식으로 PERL 사용

// type(of: )처럼 스몰 토크를 위주로 함수의 사이에 괄호를 연다.

// 배열 기능 탑재 ex) let arr = [ "Hello", "World" ]

// dictionary기능 : 값을 빨리 찾기 위한 자료구조. 자료 순서 무관
// key를 입력하면 값을 출력

a += 1 // let a를 var a로 변경

// a = 1.0 swift는 type-strict하기 때문에 자료형대로 써주어야 한다.

let arr = [ "1", "b", "c" ]

//arr[2] = "hello"
//arr.append("Hello")
//위의 둘은 var형을 사용하지 않아 오류


